<%= form_for @webpage do |t| %>
  <%= t.label :title, 'Title (optional)' %>
  <%= t.text_field :title, class: 'field' %>

  <%= t.label :url, 'URL' %>
  <%= t.text_field :url, class: 'field' %>

  <%= t.submit 'Submit', class: 'button' %>
<% end %>
